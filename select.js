// selecting function for dev branch commit

const select = (arr) => {
  const newArr = new Array();
  for (let i = 0; i < arr.length; i++) {
    if (arr[i] === condition) {
      newArr.push(arr[i]);
    }
  }
  return newArr;
};
